"""Odoo -> AVIV ``buildingProperty`` block (new-builds program, co-promotion).

Placeholder. Used only by the SLN/SLC portals; introduce when the field set
gains a "programme neuf" concept.
"""

from __future__ import annotations

from typing import Any


def build_building_property(template: dict[str, Any]) -> dict[str, Any] | None:
    return None
