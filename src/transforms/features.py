"""Odoo -> AVIV ``features`` block (residential / parking / security / internet).

Placeholder until the starter field set grows to cover amenities. Returning
``None`` means the orchestrator omits the block entirely.
"""

from __future__ import annotations

from typing import Any


def build_features(template: dict[str, Any]) -> dict[str, Any] | None:
    return None
