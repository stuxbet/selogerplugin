# SeLoger ↔ Odoo connector

Odoo ↔ SeLoger listing sync. Sibling project to [../immoweb-test](../immoweb-test/),
which connects Odoo to Immoweb (same parent company). Same connector pattern,
different downstream API.

Empty starting point — to be scaffolded.
